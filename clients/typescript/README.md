# messaging-client (TypeScript)

CLI messaging client generated from `spec/spec.md`.

## Build

    npm install
    npx tsc

## Run

    node dist/client.js

Then type `/login <name>`, `/help`, or `/quit`.
